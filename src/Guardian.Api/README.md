# Guardian.Api

ASP.NET Core Web API (.NET 8) com Swagger, EF Core (SQLite), LINQ e integrações externas (BCB Selic).

## Executar
```bash
dotnet run
# Swagger em http://localhost:5000/swagger (ou https://localhost:5001/swagger)
```
